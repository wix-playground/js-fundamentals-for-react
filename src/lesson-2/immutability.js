const todoState = {
    someId : {
        task: 'Finish the tutorial',
        isDone: false
    }
}

export function updateTask(state, todoId, task) {
    return {};
}

// your code goes here:
export function toggleDone(state, id) {
    // @TODO: create a new todos object, with a toggled isDone:
    return {}
}













// end